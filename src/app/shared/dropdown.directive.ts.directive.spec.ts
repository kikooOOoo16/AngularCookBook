import {Dropdown

.
Directive.TsDirective
}
from
'./dropdown.directive.ts.directive';

describe('Dropdown.Directive.TsDirective', () => {
  it('should create an instance', () => {
    const directive = new Dropdown.Directive.TsDirective();
    expect(directive).toBeTruthy();
  });
});
